{
  "name": "job-scraper-bot",
  "private": true,
  "description": "Deterministic digest pipeline: scrape via the job-scraper MCP server, render the digest, post it to Discord, record the run.",
  "type": "commonjs",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "digest": "node run-digest.js",
    "test": "node --test",
    "test:samples": "node --test lib/requirements.golden.test.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "1.30.0",
    "undici": "7.29.1"
  },
  "devDependencies": {
    "mongodb": "7.6.0"
  }
}
