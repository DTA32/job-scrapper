# One-shot CLI scraper (local testing / debugging).
# Requires the shared base built first:  docker build -f Dockerfile.base -t job-scraper-base .
FROM ghcr.io/pt-sinan-akbar/job-scraper-base:c908aa76 AS scraper-cli

ENTRYPOINT ["python", "-m", "scraper"]
CMD []
