services:

  # Delete this to activate the cron schedule job
  bot:
    entrypoint: ["sleep"]
    command: ["infinity"]

  # override: mount merged dev config (generated by run_dev.sh)
  scraper-mcp:
    volumes:
      - /tmp/config.dev.yaml:/app/config.yaml
